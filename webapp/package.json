{
  "name": "zk-worth",
  "version": "1.0.0",
  "main": "server.js",
  "private": true,
  "type": "module",
  "dependencies": {
    "ethers": "^5.7.2",
    "express": "^4.18.2",
    "express-async-handler": "^1.2.0"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20"
  },
  "scripts": {
    "deps-dev-install": "yarn install && cd frontend && yarn install",
    "server": "nodemon -r dotenv/config server.js",
    "fullstack": "cd frontend && yarn build && cd ../ && nodemon -r dotenv/config server.js",
    "client": "cd frontend && yarn start",
    "dev": "concurrently \"yarn server\" \"yarn client\""
  }
}
